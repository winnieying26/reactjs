// 1 == "1" => true
// 1 === "1" false

// double ==  compare null

console.log(null === null);

// null == undefined  true
/*The == (loose equality) and === (strict equality) operators are used 
for comparisons in JavaScript, but they behave differently regarding 
type conversion and value checking.
== performs type coersion , compares values after attempting to convert them to the same type
=== comapares both type and values directly
*/ 
